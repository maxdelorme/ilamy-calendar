# Development Log - 2026-05-20

## Changes

- **[recurrence / scope this]**: Fixed duplicate display when drag-and-drop edits an already detached override with scope `"this"`. Stored overrides keep `targetEvent.recurrenceId` and are **updated in place**; generated instances get a **new** detached row.
- **[recurrence / clarity]**: Replaced filter-and-repush (`working`) with explicit branches: `!isOverrideEvent` → push new override, `isOverrideEvent` → update existing row by `targetEvent.id`.
- **[recurrence / types]**: Narrowed `recurrenceId` to always be `string` for EXDATE handling (`targetEvent.recurrenceId ?? targetEvent.start.toISOString()` when `isOverrideEvent`).
- **[tests]**: Regression asserts second scope-`this` on detached override keeps the same `id` and a single override row.
- **[log sync]**: Refreshed after latest `recurrence-handler` edits and `bun run type-check` pass.

## Files Modified

- `src/features/recurrence/utils/recurrence-handler.ts` - Scope `"this"` add-vs-update-in-place for detached overrides; `recurrenceId` typing for EXDATEs
- `src/features/recurrence/utils/crud-operations.test.ts` - `should update stored override in place when scope this targets detached instance (no duplicate rows)`
- `docs/logs/2026-05-20.md` - This log entry

## Notes

Re-editing a detached override must not push a second row; updating by `targetEvent.id` preserves one stored override per occurrence key. `bun run type-check` passes after the session changes.
